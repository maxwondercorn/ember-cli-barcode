<div class="form-group float-left">
  <label class="text-info">
    {{@label}}
  </label>
  <select {{on 'change' (fn @update @label)}}>
    {{#each @items as |item|}}
      <option value={{item.value}} selected={{item.selected}}>
        {{item.value}}
      </option>
    {{/each}}
  </select>
</div>